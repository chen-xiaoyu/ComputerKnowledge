# 1. 用户及文件管理-权限表示
9位，三位一组，分布表示文件所有者、同组用户和其他用户对该文件的权限。r(读，4)w(写，2)z(执行，1)-(禁止，0)
u:文件所有者,g：同组用户,o：其他用户,a：所有用户
# 2. 更改文件属性
chown:改变文件的所有权
chgrp:改变文件分组
chmod:改变文件权限
格式：chomd a-w+x mydir 所有用户去掉权限加上可执行权限
# 3.常见命令
su: 用户切换，rm: 文件删除，tar:文件压缩
