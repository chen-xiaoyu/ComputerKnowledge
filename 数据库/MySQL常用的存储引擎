存储引擎就是指表的类型以及表在计算机上的存储方式。是 mySQL 才有的概念。

MyISAM
基于非聚簇索引，该搜索引擎不支持事务、也不支持外键，所以访问速度较快。当对事务完整性没有要求并以访问为主的应用适合使用这种存储引擎.

InnoDB
基于聚簇索引实现，索引和数据唯一。一个 B+树中，以主键为索引 key 值。因此主键最好递增。支持事务，支持具有提交、回滚及崩溃回复能力等事务特性。比 MyISAM 存储引擎占有更多磁盘空间，因此当需要大量频繁的更新删除操作，同时还对事务的完整性要求较高，需要实现并发控制时使用。

MEMORY
基于 hash 索引实现，所以速度非常快。当数据库重启时，数据会丢失，因为是存在内存中的，适合于需要快速的访问或临时表。
